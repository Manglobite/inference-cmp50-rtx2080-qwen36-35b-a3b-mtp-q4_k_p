# Что на что влияет: матрица «параметр → эффект»

[English](WHAT_INFLUENCES_WHAT.md) | **Русский**

Все цифры ниже измерены на эталонном стенде (см. `HARDWARE.ru.md`) с
Qwen3.6-35B-A3B Q4_K_P, контекст 262 144, Q8_0 K/V, Flash Attention, один слот,
если не указано иное. «decode» — серверный `eval` (кейс decode-500), «prefill» —
серверный `prompt eval` для холодного промпта ~3k токенов (`prefill-4k`),
«repo» — промпт репозитория 26 319 токенов без prompt-кэша.

## Краткая матрица

| Параметр | Влияет на | Не влияет на | Доказательство | Рекомендация |
| --- | --- | --- | --- | --- |
| `GGML_CUDA_DISABLE_DP4A=ON` (PR #25834) | квантованный decode (+70–80% на Q4/Q5 одной CMP, +13.9% на трёх-GPU Q4) | prefill (путь MMQ), Q8-модели (~+2%) | `docs/cmp50hx-dp2a-df03399-result.md` | включать для Q4/Q5 на CMP 40/50HX |
| `nvcc -fmad=false` | FP32 CMP: decode +15.0% к DP2A, prefill +15.7%; CMP-контроль +29.6% | acceptance, байты вывода | `docs/cmp50hx-dp2a-no-fmad-df03399-result.md` | включать вместе с DP2A на CMP Turing; вредно на Ampere |
| Порядок устройств = RTX хвост (`CUDA0,CUDA2,CUDA1`) | decode +54% (драфт и output head на RTX, однодевайсный драфт-граф) | байт-идентичность вывода | `docs/cmp50hx-rtx-tail-mtp-df03399-result.md` | использовать на смешанных CMP+RTX |
| `--tensor-split 1,1,2.5` | баланс VRAM и prefill; 1:2.5,1 == 1:2.25,1; 1:2.75,1 == 1:3,1 | decode в пределах ~1% | отчёт по split | 1,1,2.5 с RTX-хвостом; 1,3,1 если RTX должен быть в середине |
| MTP `--spec-draft-n-max` | decode: 1/2/3/4 → 84.5/93.6/95.5/86.5 (короткий ctx) и 66.5/68.3/82.3/84.2 (repo-28k) | prefill | отчёт по RTX-хвосту | 3 (по умолчанию), 4 — только для длинного контекста |
| Размещение MTP (`-ot blk.40.*=CUDA1` отдельно) | decode **−16.7%**: 16 сплитов планировщика на драфт-шаг | acceptance (не меняется) | отчёт по split/MTP | никогда не отрывать MTP от хвоста; двигать весь хвост |
| `--ubatch-size` | только prefill; немонотонно: 256/320/384/448/512 → 456/530/499/**546**/542 tok/s на `1,3,1`; 433 лучший из 56–512 на `1,2.25,1`; 512 падает по OOM там же | decode | таблицы в отчётах | 448 на принятом split; проверяйте запас VRAM |
| `--batch-size` | только логический предел батча | decode | разбор кода | держать 1024 |
| Размер контекста | память KV: 2.92 ГиБ при 262k → 4.11 ГиБ при 360k | скорости prefill/decode (идентичны) | отчёт по лесенке | 262144 нативно; 368640 работает, но с оговоркой RoPE |
| Prompt cache (`--cache-prompt`, `--cache-ram`, `--cache-idle-slots`) | повторы: 4 пересчитанных токена; двухсессионная выгрузка KV в ОЗУ и обратно | стоимость первого запроса | отчёт по лесенке | держать включённым |
| `GGML_CUDA_GRAPH_OPT=1` | здесь ничего измеримого (−1.5%) | | отчёт по no-fmad | пропустить |
| `GGML_CUDA_P2P=1` | здесь ничего измеримого (−0.5%) | | отчёт по no-fmad | пропустить |
| `--spec-type ngram-mod` | 76.1 tok/s на echo-промпте против 110.4 у MTP | | отчёт по RTX-хвосту | пропускать, пока есть MTP |
| `--threads` | только токенизация/батчинг | decode | заметки фазы 0 | 4 на этом 24-поточном хосте |

## Почему эти ручки работают (механизм)

1. **DP4A против DP2A (`GGML_CUDA_DISABLE_DP4A`).** CMP 50HX умеет DP4A, но
   исполняет её примерно за 33 цикла вместо обычных ~2; DP2A на той же карте —
   ~2.25 цикла. Патч заменяет один DP4A на две перестановки PRMT и два DP2A в
   `ggml_cuda_dp4a`, который используется в decode MMVQ и в vec-dot
   FlashAttention с квантованным KV. Prefill не затронут, потому что на Turing
   квантованные матмулы идут через MMQ, а его int8-MMA путь не вызывает этот
   helper. Поэтому prefill не изменился, а decode вырос.
2. **Троттлинг FMA (`-fmad=false`).** Тот же класс CMP исполняет FP32 FMA на
   ~0.43 TFLOPS, тогда как раздельные mul+add дают ~6.88 TFLOPS. Флаг запрещает
   NVCC сворачивать mul+add в FMA. На обычных GPU это удваивает число
   FP32-инструкций и может вредить; на RTX-стадии здесь decode упирается в
   память, поэтому суммарный эффект всё равно положительный. Флаг передаётся
   NVCC при сборке (`-DCMAKE_CUDA_FLAGS=-fmad=false`), а не в рантайме, и
   выигрывает даже когда `-use_fast_math` стоит позже в командной строке.
3. **Порядок устройств и хвост.** При layer split каждый токен проходит все
   стадии по порядку, и хвостовое устройство считает output head и (для этой
   модели) MTP-слой. Когда хвост на RTX, драфт-граф MTP однодевайсный: драфт
   считается целиком на самой быстрой карте без лишних сплитов; когда хвост на
   CMP, драфт считается на этой CMP. Принудительный перенос MTP-тензоров на
   RTX при хвосте на CMP дробит драфт-граф на 16 частей и теряет 16.7%.
4. **ubatch.** Большие физические батчи нужны только prefill; decode идёт по
   1–2 токена на шаг. Больший ubatch улучшает загрузку ядер, но увеличивает
   compute-буферы и конкурирует с KV за память 10-гиговых CMP. Кривая
   немонотонная (пороги конфигураций ядер), поэтому её нужно свипать на своём
   железе, а не копировать число.
5. **Глубина MTP.** Более глубокий драфт стоит немного дороже на шаг, но может
   принять больше токенов за шаг. Acceptance падает (82% → 71% → 57%, средняя
   длина драфта 1.8 → 3.1 → 3.3 при n-max от 1 до 4), а оптимум зависит от
   длины контекста: n-max=3 на коротком, n-max=4 на длинном.
6. **Контекст и KV.** Модель гибридная (11 из 41 слоя — full attention,
   остальные — linear attention с фиксированным рекуррентным состоянием).
   Только 11 attention-слоёв масштабируют KV с контекстом: 2.92 ГиБ при 262k,
   4.11 ГиБ при 360k для Q8_0 K/V. Поэтому 360k влезает без смены split.

## Какие тесты проводились

| Тест | Цель | Метод | Где |
| --- | --- | --- | --- |
| Детерминированный smoke | доказать, что патч не меняет математику | запрос на 128 токенов, temperature 0, побайтовое сравнение | `docs/cmp50hx-dp2a-*.md` |
| Чередующийся A/B | честное сравнение пропускной способности | 3–5 чередующихся циклов на вариант, один сервер на прогон | все отчёты |
| CMP-контроль (DeepSeek Q4) | отделить эффект CMP от RTX | тот же протокол на `CUDA_VISIBLE_DEVICES=0`, RTX простаивает | отчёты DP2A/no-fmad |
| Свипы ubatch | найти оптимум prefill и край OOM | `prefill-4k` и холодный `repo-28k`, по 2 точки на значение | отчёт по split/ubatch |
| Свипы split | сбалансировать слои и освободить VRAM | те же кейсы, разные `--tensor-split` | отчёты по split и RTX-хвосту |
| Глубина и размещение MTP | максимизировать спекулятивный decode | decode-500 и repo-28k, acceptance из логов | отчёт по RTX-хвосту |
| Диагностика планировщика | объяснить регрессию пина MTP | `GGML_SCHED_DEBUG=1` + `-lv 5`, подсчёт секций сплитов | отчёт по split/MTP |
| Контекстные лесенки | prompt cache, prefill, генерация против контекста | hello → 10k → 75k → повтор, одна и две сессии, 262k/360k | отчёт по лесенке |
| Агентные/дизайн-сессии | практическая польза и работа с инструментами | сценарные диалоги с детерминированной эмуляцией инструментов | отчёты по дизайн-сессиям |

## Как измерить то же самое на своём железе

1. Всегда привязывайтесь по порядку PCI и ведите телеметрию; записывайте
   raw-тайминги сервера (`prompt eval time`, `eval time`) и HTTP end-to-end.
2. Для любого сравнения требуйте байт-идентичные completion при temperature 0;
   ускорение с изменившимся выводом — это другой эксперимент.
3. Меняйте одну ось за раз; чередуйте A и B внутри одной сессии, а не «сначала
   все A, потом все B».
4. Свипайте ubatch на своём стенде: это самое железозависимое число здесь.
5. После каждого изменения проверяйте запас VRAM; узкая карта — та, что упадёт
   по OOM на более длинном промпте.
