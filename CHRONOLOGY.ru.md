# Хронология: что пробовали, почему и что получилось

[English](CHRONOLOGY.md) | **Русский**

Здесь короткая история проекта. Для каждого шага указаны причина («почему»),
действие и измеренный результат. Негативные результаты оставлены специально:
они экономят время другим людям.

## Фаза 0 — одиночная CMP 50HX (июль – август 2026)

- **Почему**: в хосте две майнинговые CMP 50HX и одна RTX 2080 Ti; первый
  вопрос — годятся ли CMP для локального инференса.
- **Что**: введена строгая привязка (`CUDA_DEVICE_ORDER=PCI_BUS_ID`,
  `CUDA_VISIBLE_DEVICES=0`, `--device CUDA0`, `gpu-binding.json`,
  `gpu-telemetry.csv`). Измерены малые модели: Qwen2.5-Coder-3B Q8_0
  (95.8 tok/s decode-300), Qwen3.5-4B Q8_0 (66.9), DeepSeek-R1-8B Q4_K_M
  (29.4), Seed-Coder-8B Q5_K_M (28.5). Flash Attention важен для длинных
  контекстов; prompt cache работает на повторных запросах.
- **Итог**: CMP пригодны, но медленные; главные ограничения — 10 ГиБ и prefill
  на длинном контексте.

## Фаза 1 — DP2A и A/B на одной CMP (11.08.2026)

- **Почему**: CMP 50HX заявляет sm_75, но исполняет DP4A медленно; issue #24616
  предложил заменить один DP4A на два DP2A плюс перестановки PRMT
  (PR #25834, коммит `c499d2f4`).
- **Что**: собраны baseline и DP2A из `86d86ed` с одинаковыми флагами;
  чередующийся A/B на изолированной CMP.
- **Итог**: DeepSeek Q4 +79%, Seed Q5 +77%, Qwen Q8 +2% — DP2A принят только
  для Q4/Q5, Q8-профили остались на baseline. Здесь же закреплён протокол
  всех последующих тестов (байт-идентичные completion, привязка, телеметрия,
  пять чередующихся пар).

## Фаза 2 — цель Qwen3.6-35B на трёх GPU (15.09.2026)

- **Почему**: рабочей моделью стала Qwen3.6-35B-A3B Q4_K_P (24.3 ГБ) на двух
  CMP и RTX (`--tensor-split 1,2.25,1`), контекст 262 144, Q8_0 K/V, Flash
  Attention, MTP `n-max=1`. Существующий DP2A-рантайм был собран из `86d86ed`,
  а не из продового `df03399`.
- **Что**: патч PR портирован на `df03399` (только дрейф контекста; 22
  вставки; SHA патча `dd73bdea...`); под memory-guard собраны парные
  baseline и DP2A.
- **Итог**: decode-500 47.79 → 54.44 tok/s (+13.9%), HTTP +12.9%, prefill не
  изменился (373.61 → 373.63), repo-28k decode +12%, всё байт-идентично, VRAM
  тот же. CMP-контроль DeepSeek воспроизвёл +69.5%. Прод переведён на DP2A
  (`docs/cmp50hx-dp2a-df03399-result.md`).

## Фаза 3 — `-fmad=false` против троттлинга FP32 (16.09.2026)

- **Почему**: внешние данные по CMP 50HX: FP32 FMA зарезан примерно в 16 раз
  относительно раздельных mul+add, значит `nvcc -fmad=false` должен помочь
  сверх DP2A.
- **Что**: третий рантайм с тем же патчем плюс
  `-DCMAKE_CUDA_FLAGS=-fmad=false`; проверено, что флаг переживает
  `-use_fast_math` (локальный зонд nvcc: FFMA=0). Трёхсторонний A/B плюс
  CMP-контроль, плюс тесты env-кнобов и ubatch.
- **Итог**: decode-500 47.78 → 54.04 → **62.17** tok/s
  (no-fmad +15.0% к DP2A, +30.1% к baseline); prefill-4k raw
  372.69 → 431.78 (+15.7%); repo-28k decode +7.4%; CMP-контроль
  27.91 → 47.27 → **61.27** (+119.5% к baseline). `GGML_CUDA_GRAPH_OPT=1`
  (−1.5%) и `GGML_CUDA_P2P=1` (−0.5%) нейтральны. ubatch 512 падает по OOM на
  старом split. Принят no-fmad (`docs/cmp50hx-dp2a-no-fmad-df03399-result.md`).

## Фаза 4 — split, ubatch и размещение MTP (16.09.2026)

- **Почему**: узким местом оставались CMP; больший ubatch должен ускорять
  prefill, а MTP-голова, предположительно, грузила CMP.
- **Что**:
  1. **Эксперимент с пином MTP**: `-ot 'blk\.40\..*=CUDA1'` при хвосте на CMP.
  2. **Сравнение split**: `1,2.5,1`, `1,2.75,1`, `1,3,1`.
  3. **Свип ubatch** `56/64/128/256/320/384/448/512` на prefill-4k и на
     холодном 26 319-токенном промпте репозитория.
- **Итог**:
  - Пин MTP технически работает, но decode падает на **−16.7%** при том же
    acceptance. `GGML_SCHED_DEBUG=1` показывает рост драфт-графа с 2 до 16
    сплитов (+~6 мс на драфт-шаг). **MTP должен быть на одном устройстве с
    хвостом.**
  - `1,2.5,1` = `1,2.25,1`, `1,2.75,1` = `1,3,1` по дискретной раскладке;
    `1,3,1` освобождает ~1 ГиБ и позволяет ubatch 512. На `1,3,1`:
    prefill-4k 456/530/499/**546**/542 tok/s и repo
    387/455/432/**475**/469 для 256/320/384/448/512 → **лучший 448**.
  - Промежуточное внедрение: `1,3,1` + ub448 (prefill +26%, decode −1.1%).
  - `docs/cmp50hx-split-ubatch-mtp-df03399-result.md`

## Фаза 5 — RTX хвостовым и глубокая MTP (16.09.2026)

- **Почему**: пин MTP провалился из-за разрыва драфта по устройствам. Если же
  перенести на RTX весь хвост (последние слои + output head + MTP), драфт
  остаётся однодевайсным и считается на самой быстрой карте.
- **Что**: порядок устройств `CUDA0,CUDA2,CUDA1` со split `1,1,2.5`; свип
  `1,1,3 / 1,1,2.5 / 1,1,2.25 / 1,1,2`; глубина MTP `n-max 1/2/3/4`; сравнение
  с n-граммами (`ngram-mod`) на echo-промпте.
- **Итог**: decode-500 61.28 → **95.47** tok/s (+54% к прежнему прод-у и
  +99% к исходному baseline), repo-28k decode 82.3, prefill-4k 465.8 и repo
  prefill 405.4 (оба выше исходного профиля). Лучший баланс — `1,1,2.5`.
  Глубина MTP: 84.5 / 93.6 / **95.5** / 86.5 tok/s на decode-500 и
  66.5 / 68.3 / 82.3 / 84.2 на repo-28k для n-max 1/2/3/4 → выбран
  **n-max=3** (n-max=4 лучше только на длинном контексте). `ngram-mod` на echo
  даёт 76.1 против 110.4 у MTP → отклонён.
  `docs/cmp50hx-rtx-tail-mtp-df03399-result.md`.

## Фаза 6 — контекстные лесенки 262K и 360K (16.09.2026)

- **Почему**: прод — это длинный контекст; нужно понять поведение prefill,
  prompt-кэша и генерации по мере роста сессии и проверить 360k.
- **Что**: лесенка `hello → 10k → 75k → повтор 75k` в одной и двух сессиях
  с выгрузкой KV в ОЗУ; то же при ctx 368 640.
- **Итог**: точный повтор — 4 пересчитанных токена (~100% кэш); префикс 10k
  переиспользуется внутри 75k (12.5%); prefill 473 tok/s на 10k, ~331 tok/s
  эффективно для холодного 75k (~230 с); генерация ~60 tok/s при 75k занятого
  контекста, ~46 tok/s в устойчивом режиме двух сессий. 360k влезает без
  перераспределения слоёв (attention KV 2.92 → 4.11 ГиБ; пик RTX 21 940 МиБ).
  Swap дошёл до 3.98 ГиБ в двухсессионном прогоне 360k.
  `docs/cmp50hx-context-ladder-262k-360k.md`.

## Фаза 7 — дизайн-сессии в агентном режиме на 360K (16–17.09.2026)

- **Почему**: практическая проверка, годится ли модель как агент: вызовы
  инструментов, длинные диалоги, хаотичный нетехнический пользователь.
- **Что**: сессия проектирования микроблога, затем два Go-приложения (сервис
  коротких ссылок и трекер расходов) с намеренно хаотичным пользователем;
  инструменты `run_sqlite`, `write_file`, `read_file`, `calc` исполнял harness.
- **Итог**: модель не теряла задачу, возражала против плохих идей, честно
  говорила о невозможном, выдала парсибельные Go-каркасы; найдены мелкий дрейф
  и дыры в коде (у сокращателя не зарегистрирован redirect-обработчик, у
  расходов игрушечный cookie-логин и смешение валют).
  `docs/cmp50hx-design-session-360k.md`,
  `docs/cmp50hx-design-sessions-go-2apps.md`.

## Итоговая принятая конфигурация

```text
рантайм:        llama.cpp df03399 + порт PR #25834 + -fmad=false
устройства:     --device CUDA0,CUDA2,CUDA1        (RTX — хвост)
split:          --tensor-split 1,1,2.5
контекст:       262144 (368640 проверен и работоспособен)
кэш:            K/V q8_0, Flash Attention вкл.
MTP:            --spec-type draft-mtp --spec-draft-n-max 3
батчинг:        --batch-size 1024 --ubatch-size 448
prompt cache:   --cache-prompt --cache-ram 8192 --cache-idle-slots
```

Суммарный эффект против нетронутого DP4A baseline на том же стенде:
decode-500 47.8 → 95.5 tok/s (**×2.0**), prefill-4k 372.7 → 465.8 tok/s
(**+25%**), плюс рабочий контекст 360k, ко-локация MTP и переиспользование
prompt-кэша.
