# Железо и ПО эталонного стенда

[English](HARDWARE.md) | **Русский**

## Хост

| Компонент | Значение |
| --- | --- |
| CPU | Intel Xeon E5-2670 v3 @ 2.30 ГГц, 12 ядер / 24 потока, 30 МиБ L3, один сокет |
| RAM | 30 ГиБ (32 165 012 КиБ по `/proc/meminfo`) |
| Диск | TOSHIBA TR200 223 ГБ SATA SSD; корневой LV 98 ГБ (в тестах ~25 ГБ свободно) |
| ОС | Ubuntu 26.04 LTS, ядро 7.0.0-31-generic |
| Драйвер NVIDIA | 595.71.05 (поддерживает CUDA runtime 13.2) |
| Сборочный тулчейн | CUDA 12.4 (nvcc V12.4.131), GCC 15.2.0, CMake 4.2.3 |
| NCCL | 2.22.3 (`/usr/lib/x86_64-linux-gnu/libnccl.so.2`) |
| Python | 3.14 (harness), numpy не требуется |

## GPU

Порядок CUDA-устройств ниже — это **порядок PCI**, используемый во всём кейсе
(`CUDA_DEVICE_ORDER=PCI_BUS_ID`, `CUDA_VISIBLE_DEVICES=0,1,2`).

| Индекс | Модель | PCI bus | UUID | VRAM | Compute | Power limit | Роль в принятом профиле |
| ---: | --- | --- | --- | ---: | ---: | ---: | --- |
| CUDA0 | NVIDIA CMP 50HX | `00000000:03:00.0` | `GPU-201703f9-1d0f-8a3d-9fa8-3688691d6d2a` | 10 240 МиБ | sm_75 | 225 Вт | первая стадия слоёв |
| CUDA1 | NVIDIA GeForce RTX 2080 Ti | `00000000:04:00.0` | `GPU-6a6576bd-9d4b-c9e1-7a90-a03484daf586` | 22 528 МиБ | sm_75 | 260 Вт (макс 320) | **хвостовая стадия** |
| CUDA2 | NVIDIA CMP 50HX | `00000000:08:00.0` | `GPU-362c8abe-08a4-fd4d-b14b-218b26b65d80` | 10 240 МиБ | sm_75 | 225 Вт | средняя стадия слоёв |

UUID других экземпляров CMP 50HX могут отличаться; свои смотрите через `nvidia-smi -L`.

### Топология

- Все пары GPU показывают `PHB` (через хост-мост PCIe); **NVLink отсутствует**.
- Один NUMA-узел (CPU 0-23).
- `nvidia-smi` в простое показывает линки Gen1 x1 (энергосбережение); максимум —
  Gen2 x16 у CMP и Gen3 x16 у RTX. При layer split по PCIe передаются только
  небольшие активации, так что PCIe не является узким местом decode.

### Особенности CMP 50HX (внешние + локальные данные)

- Латентность DP4A ≈ 33.08 циклов против ≈ 2.25 у DP2A (микробенчмарк
  `arabel1a/gpu-micro-bench`), при том что карта заявляет sm_75.
- Пропускная способность FP32 FMA зарезана: около 0.43 TFLOPS с FMA против
  примерно 6.88 TFLOPS при разделении на mul+add (`nvcc --fmad=false`),
  измерено внешне (Habr, WebSlave) и подтверждено нашими A/B.
- Те же обходы вредят другим архитектурам: у RTX 3060/CMP 90HX FP32 падает
  примерно вдвое при `--fmad=false`; CMP 100/170 нужны другие приёмы. Кейс
  специфичен для Turing-класса CMP 40HX/50HX.

## Почему именно три GPU

- Модель 24.3 ГБ Q4_K_P не влезает в одну 10-гиговую CMP и не помещается
  целиком в RTX (~22 ГиБ вместе с 262k Q8 KV).
- Три стадии дают достаточный суммарный объём VRAM (42.5 ГиБ) и ставят
  большую быструю RTX на самую дорогую стадию (хвост с output head и MTP).
- `row` split на CUDA-бэкенде CMP не поддерживается; рабочий режим —
  `layer` с упорядоченным по устройствам `--tensor-split`.

## Минимум для повторения кейса

- Две CMP 50HX (или CMP 40HX) по 10 ГиБ и одна большая Turing-карта ~22 ГиБ
  (RTX 2080 Ti / Titan RTX). Все должны быть sm_75 — иначе выигрыш от DP2A и
  `-fmad=false` не действует.
- ≥ 42 ГиБ суммарной VRAM для профиля 262k; 360k влезает, но оставляет ~0.6 ГиБ
  на RTX.
- 30 ГиБ системной RAM (prompt cache до 8 ГиБ; в тесте 360k на двух сессиях
  ушло 4 ГиБ swap).
- ~40 ГБ свободного диска: 24.3 ГБ модель + 0.9 ГБ проектор + ~10 ГБ сборка.
- CUDA 12.4 для сборки (готовый рантайм собран ей); новые тулкиты, скорее
  всего, совместимы, но не проверялись.
- Материнская плата с тремя слотами PCIe (даже x4/x8 хватает для layer split).

## Что меняется на другом железе

| Отличие | Ожидаемый эффект |
| --- | --- |
| CMP 40HX вместо 50HX | Тот же класс троттлинга DP4A/FMA; направление похожее, цифры другие. |
| Одна большая GPU + одна CMP | Порядок `CUDA0,CUDA1` с меньшим split; 262k может не влезть. |
| Ampere CMP (90HX/170HX/100) | **Не** использовать `-fmad=false`; DP4A-патч неприменим к CMP100/170 (другой механизм). |
| Современная GPU ≥48 ГБ | Предпочесть одну карту; приёмы кейса не нужны. |
| NCCL не установлен | Собирать без `-DGGML_CUDA_NCCL=ON`; на этом стенде эффект был ~0.3%. |
